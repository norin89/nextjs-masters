fragment Cart on Cart {
    # TODO: Should be `...Cart` but can't due to API limitations
    id
    items {
        quantity
        product {
            id
            name
            price
            images {
                url
                alt
            }
        }
    }
}
