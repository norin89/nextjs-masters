query CartGetById($id: ID!) {
    cart(id: $id) {
        # TODO: Should be `...Cart` but can't due to API limitations
        id
        items {
            quantity
            product {
                id
                name
                price
                images {
                    url
                    alt
                }
            }
        }
    }
}
