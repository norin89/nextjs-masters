mutation CartRemoveItem($id: ID!, $productId: ID!) {
    cartRemoveItem(
        id: $id
        productId: $productId,
    ) {
        # TODO: Should be `...Cart` but can't due to API limitations
        id
        items {
            quantity
            product {
                id
                name
                price
            }
        }
    }
}
